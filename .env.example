# Frontend Environment Variables
# Copy this file to .env.local and fill in your values

# API URL for the backend HTTP server (default: http://localhost:8787)
# This is where the frontend will send requests for alpha generation and backtesting
VITE_API_URL=http://localhost:8787

# Note: Do NOT put API keys in the frontend .env file!
# API keys should only be in the MCP server environment for security.
