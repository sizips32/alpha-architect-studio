# Gemini API Key (required for AI features)
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Alternative API key name (fallback)
API_KEY=your_api_key_here

# Backend API URL (optional - defaults to http://localhost:8787)
VITE_API_URL=http://localhost:8787

# CORS Proxy URL (optional - defaults to https://cors-anywhere.herokuapp.com/)
# WARNING: Public CORS proxies are not recommended for production
# Consider setting up your own backend proxy
VITE_PROXY_URL=https://cors-anywhere.herokuapp.com/

# Server Configuration (MCP Server / HTTP Bridge)
# Port for HTTP server (optional - defaults to 8787)
PORT=8787

# Allowed CORS origins (optional - defaults to *)
# For production, specify your frontend domain(s)
# Examples: http://localhost:5173,https://yourdomain.com
ALLOWED_ORIGINS=*
